[phases.setup]
nixPkgs = ["nodejs_24"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'Backend build step complete'"]

[start]
cmd = "node --experimental-strip-types backend/src/server.ts"
