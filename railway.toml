[build]
builder = "NIXPACKS"
buildCommand = "echo 'Backend ready'"

[deploy]
startCommand = "node --experimental-strip-types backend/src/server.ts"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
